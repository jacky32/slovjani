Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  RewriteCond %{REQUEST_URI} !^/index\.php$

  # Route all app requests through the front controller, including /public/*.
  RewriteRule ^ index.php [L,QSA]
</IfModule>
